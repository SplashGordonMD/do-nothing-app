<h1>All Contacts</h1>
<% @contacts.each do |contact| %>
	<h2>Name:<a href="/contacts/<%= contact.id %>"><%= contact.name %></a></h2>
	<h3>Phone: <%=contact.phone %></h3>
	<h4>City: <%=contact.city %></h4>
	<a href="/contacts/<%= contact.id %>"><%= contact.name %></a>
	<% end %>